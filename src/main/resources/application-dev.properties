# Development-only JWT secret.
# This value is safe to commit — it is never used outside local machines.
security.jwt.secret=dGhpcyBpcyBhIHNlY3VyZSBzZWNyZXQga2V5IGZvciBqd3QhISE=

# Drop and recreate the schema on every restart in dev only.
spring.jpa.hibernate.ddl-auto=create

# Enable the H2 web console at /h2-console (dev only).
spring.h2.console.enabled=true

# Log SQL statements in dev to aid debugging.
logging.level.sql=debug
