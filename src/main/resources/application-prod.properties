# Production JWT secret — MUST be supplied as an environment variable.
# Never commit a real secret to source control.
#
# Generate a strong value with: openssl rand -base64 32
# Then run the application with: JWT_SECRET=<value> ./run.sh
security.jwt.secret=${JWT_SECRET}
